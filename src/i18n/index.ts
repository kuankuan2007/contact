import I18nInstance from '@kuankuan/assist-2026/i18n';
import messages, { langs } from 'virtual:k-i18n-config:main';

const i18nInstance = new I18nInstance(messages, langs);

export default i18nInstance;
